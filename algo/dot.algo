PROCEDURE dot(v1,v2:ARRAY_OF INTEGER , VAR ps:INTEGER)
VAR
    i:INTEGER
BEGIN
   IF(v1.length<> v2.length) THEN{
    Write("we can not calculate")
}ELSE{
    FOR i FROM 0 TO v1.length-1 STEP step  DO
    ps=ps+v1[i]*v2[i]
    END_FOR
    
    }
END

ALGORITHM algorithm_name
VAR
   v1,v2: ARRAY_OF INTEGER [10];
   ps:INTEGER
BEGIN
    ps=0
  dot(v1,v2,ps)
  if(ps=0){
      Write("orthogo")
  }
END